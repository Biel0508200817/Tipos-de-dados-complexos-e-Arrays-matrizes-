/**
 * ### **5️⃣ Usando `pop`**

Crie um array `alunos` com três nomes. Remova o último nome usando `pop` e exiba o array atualizado.
 */
const alunos = ['Alice', 'Bob', 'Charlie'];
alunos.pop();
console.log(alunos); // Saída: ['Alice', 'Bob']
