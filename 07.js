/**
 * ### **7️⃣ Usando `reverse`**

Crie um array `letras` com os elementos `["a", "b", "c", "d"]`. Inverta a ordem dos elementos usando `reverse` e exiba o resultado.
 */
// Criando o array letras
const letras = ["a", "b", "c", "d"];
// Invertendo a ordem dos elementos usando reverse
letras.reverse();
// Exibindo o resultado
console.log(letras);